meta {
  name: Get Me - Valid
  type: http
  seq: 8
}

get {
  url: {{base_url}}/auth/me
  headers {
    Authorization: Bearer {{access_token}}
  }
}

expect {
  res.status: 200
  res.body.user: exists
  res.body.user.email: exists
}
