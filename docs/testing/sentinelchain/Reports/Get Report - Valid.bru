meta {
  name: Get Report - Valid
  type: http
  seq: 24
}

get {
  url: {{base_url}}/reports/{{report_id}}
  headers {
    Authorization: Bearer {{access_token}}
  }
}

expect {
  res.status: 200
  res.body.id: {{report_id}}
  res.body.summaryJson: exists
}
