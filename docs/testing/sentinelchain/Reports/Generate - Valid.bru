meta {
  name: Generate Report - Valid
  type: http
  seq: 23
}

post {
  url: {{base_url}}/reports/generate
  headers {
    Authorization: Bearer {{access_token}}
  }
}

expect {
  res.status: 201
  res.body.id: exists
  res.body.summaryJson: exists
}
