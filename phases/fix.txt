The Level 1 lab, when deployed to production (Railway), has its login form
hardcoded to send the login POST request to localhost:3001 instead of the
actual current domain. This causes a CORS failure in the browser since
localhost:3001 doesn't exist for anyone visiting the real deployed site.

Find where the login form's fetch/API call is defined in the Level 1 lab's
code and fix it to use a relative URL (e.g. fetch('/login') instead of
fetch('http://localhost:3001/login')), so it automatically works whether
running locally or deployed anywhere, without needing to hardcode a domain
at all.

Check if the SAME hardcoded-localhost mistake exists in Levels 2, 3, and 4
as well, since they were very likely built from the same pattern — fix all
4 if so, not just Level 1.

After fixing, confirm by checking the actual deployed lab's login page
network request and confirming it goes to the correct current domain, not
localhost — don't just report the code looks fixed, verify the real
running site.